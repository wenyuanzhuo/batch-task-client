import * as appConfig from '../../app.json'

const serverApi = appConfig.api
const hostConfig = appConfig.hostConfig

const pingppPrivateKey = ``

export {
  appConfig,
  serverApi,
  hostConfig,
  pingppPrivateKey,
};
